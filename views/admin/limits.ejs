<%- include('layout-head', { title: 'Jogo Responsável', page: 'limits' }) %>

<div class="adm-page-card" data-page="limits">
  <div class="adm-page-head">
    <div>
      <h3>Jogo Responsável</h3>
      <p class="adm-muted">Limites de depósito, aposta, perda e auto-exclusão dos usuários</p>
    </div>
    <button class="adm-btn" data-action="refresh">
      <svg width="16" height="16" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><polyline points="23 4 23 10 17 10"/><polyline points="1 20 1 14 7 14"/><path d="M3.51 9a9 9 0 0 1 14.85-3.36L23 10M1 14l4.64 4.36A9 9 0 0 0 20.49 15"/></svg>
      Atualizar
    </button>
  </div>
  <div class="adm-toolbar">
    <div class="adm-field"><label>Buscar (user_id)</label><input type="text" placeholder="id do usuário" data-filter="q" /></div>
    <div class="adm-field"><label>Tipo de limite</label><select data-filter="limit_type"><option value="">Todos</option><option value="deposit">Depósito</option><option value="bet">Aposta</option><option value="loss">Perda</option><option value="time">Tempo</option></select></div>
    <div class="adm-field"><label>Por página</label><select data-filter="limit"><option value="10">10</option><option value="25" selected>25</option><option value="50">50</option></select></div>
    <div class="adm-field"><label>&nbsp;</label><button class="adm-btn primary" data-action="apply">Aplicar</button></div>
  </div>
  <div class="adm-table-wrap">
    <table class="adm-table">
      <thead><tr><th>ID</th><th>Usuário</th><th>Tipo</th><th>Período</th><th>Valor</th><th>Configurado por</th><th>Status</th><th>Ações</th></tr></thead>
      <tbody data-tbody></tbody>
    </table>
  </div>
  <div class="adm-pager">
    <div class="adm-muted"><span data-count>0</span> resultados</div>
    <div class="adm-pager-btns">
      <button class="adm-btn" data-action="prev">‹ Anterior</button>
      <span class="adm-muted">Página <span data-page-num>1</span></span>
      <button class="adm-btn" data-action="next">Próxima ›</button>
    </div>
  </div>
</div>

<!-- Override limit modal -->
<div id="limitModal" style="display:none;position:fixed;inset:0;z-index:9999;background:rgba(0,0,0,.55);backdrop-filter:blur(6px);align-items:center;justify-content:center">
  <div style="background:linear-gradient(160deg,#111827,#0d1321);border:1px solid rgba(255,255,255,.08);border-radius:20px;padding:32px;width:480px;max-width:92vw;box-shadow:0 24px 80px rgba(0,0,0,.6)">
    <h4 style="margin:0 0 24px;color:#fff;font-size:18px;font-weight:700;display:flex;align-items:center;gap:10px">
      <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="var(--adm-green1,#10b981)" stroke-width="2"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/></svg>
      Impor Limite ao Usuário
    </h4>
    <div style="display:flex;flex-direction:column;gap:16px">
      <div style="display:flex;flex-direction:column;gap:6px">
        <label style="font-size:11px;color:rgba(255,255,255,.45);font-weight:700;text-transform:uppercase;letter-spacing:.5px">User ID *</label>
        <input type="number" id="limUserId" placeholder="ID do usuário" style="height:42px;border-radius:12px;border:1px solid rgba(255,255,255,.1);background:rgba(0,0,0,.3);color:#fff;padding:0 14px;outline:none;font-size:13px;font-weight:500;transition:border-color .2s" onfocus="this.style.borderColor='var(--adm-green1)'" onblur="this.style.borderColor='rgba(255,255,255,.1)'">
      </div>
      <div style="display:grid;grid-template-columns:1fr 1fr;gap:14px">
        <div style="display:flex;flex-direction:column;gap:6px">
          <label style="font-size:11px;color:rgba(255,255,255,.45);font-weight:700;text-transform:uppercase;letter-spacing:.5px">Tipo</label>
          <select id="limType" style="height:42px;border-radius:12px;border:1px solid rgba(255,255,255,.1);background:rgba(0,0,0,.3);color:#fff;padding:0 14px;outline:none;font-size:13px;font-weight:500;transition:border-color .2s;cursor:pointer" onfocus="this.style.borderColor='var(--adm-green1)'" onblur="this.style.borderColor='rgba(255,255,255,.1)'">
            <option value="deposit" style="background:#1a1f2e">Depósito</option>
            <option value="bet" style="background:#1a1f2e">Aposta</option>
            <option value="loss" style="background:#1a1f2e">Perda</option>
            <option value="time" style="background:#1a1f2e">Tempo de sessão</option>
          </select>
        </div>
        <div style="display:flex;flex-direction:column;gap:6px">
          <label style="font-size:11px;color:rgba(255,255,255,.45);font-weight:700;text-transform:uppercase;letter-spacing:.5px">Período</label>
          <select id="limPeriod" style="height:42px;border-radius:12px;border:1px solid rgba(255,255,255,.1);background:rgba(0,0,0,.3);color:#fff;padding:0 14px;outline:none;font-size:13px;font-weight:500;transition:border-color .2s;cursor:pointer" onfocus="this.style.borderColor='var(--adm-green1)'" onblur="this.style.borderColor='rgba(255,255,255,.1)'">
            <option value="daily" style="background:#1a1f2e">Diário</option>
            <option value="weekly" style="background:#1a1f2e">Semanal</option>
            <option value="monthly" style="background:#1a1f2e">Mensal</option>
          </select>
        </div>
      </div>
      <div style="display:flex;flex-direction:column;gap:6px">
        <label id="limValueLabel" style="font-size:11px;color:rgba(255,255,255,.45);font-weight:700;text-transform:uppercase;letter-spacing:.5px">Valor (R$)</label>
        <input id="limValue" class="brl-mask" placeholder="R$ 0,00" style="height:42px;border-radius:12px;border:1px solid rgba(255,255,255,.1);background:rgba(0,0,0,.3);color:#fff;padding:0 14px;outline:none;font-size:13px;font-weight:500;transition:border-color .2s" onfocus="this.style.borderColor='var(--adm-green1)'" onblur="this.style.borderColor='rgba(255,255,255,.1)'">
      </div>
      <div style="display:flex;flex-direction:column;gap:6px">
        <label style="font-size:11px;color:rgba(255,255,255,.45);font-weight:700;text-transform:uppercase;letter-spacing:.5px">Observação</label>
        <input type="text" id="limNotes" placeholder="Motivo da imposição..." style="height:42px;border-radius:12px;border:1px solid rgba(255,255,255,.1);background:rgba(0,0,0,.3);color:#fff;padding:0 14px;outline:none;font-size:13px;font-weight:500;transition:border-color .2s" onfocus="this.style.borderColor='var(--adm-green1)'" onblur="this.style.borderColor='rgba(255,255,255,.1)'">
      </div>
    </div>
    <div style="display:flex;gap:10px;margin-top:24px;justify-content:flex-end">
      <button class="adm-btn" id="limCancel" style="padding:10px 20px;border-radius:10px">Cancelar</button>
      <button class="adm-btn primary" id="limSave" style="padding:10px 24px;border-radius:10px;font-weight:700">Salvar Limite</button>
    </div>
  </div>
</div>

<script src="/public/js/admin.js" defer></script>
<script>
function brlMask(input) {
  input.addEventListener('input', function() {
    var v = this.value.replace(/\D/g, '');
    if (!v) { this.value = ''; return; }
    v = (parseInt(v, 10) / 100).toFixed(2);
    this.value = 'R$ ' + v.replace('.', ',').replace(/\B(?=(\d{3})+(?!\d))/g, '.');
  });
}
function parseBRL(str) {
  if (!str) return 0;
  var n = str.replace(/[R$\s.]/g, '').replace(',', '.');
  return Math.round(parseFloat(n || 0) * 100);
}
document.querySelectorAll('.brl-mask').forEach(brlMask);

document.addEventListener('DOMContentLoaded', () => {
  const modal = document.getElementById('limitModal');
  const limType = document.getElementById('limType');
  const limValue = document.getElementById('limValue');
  const limValueLabel = document.getElementById('limValueLabel');
  var isBrl = true;

  function toggleValueMode() {
    var isTime = limType.value === 'time';
    limValue.value = '';
    if (isTime) {
      limValueLabel.textContent = 'VALOR (MINUTOS)';
      limValue.placeholder = 'ex: 60';
      limValue.classList.remove('brl-mask');
      limValue.type = 'number';
      limValue.removeEventListener('input', limValue._brlHandler);
      isBrl = false;
    } else {
      limValueLabel.textContent = 'VALOR (R$)';
      limValue.placeholder = 'R$ 0,00';
      limValue.classList.add('brl-mask');
      limValue.type = 'text';
      limValue._brlHandler = function() {
        var v = limValue.value.replace(/\D/g, '');
        if (!v) { limValue.value = ''; return; }
        v = (parseInt(v, 10) / 100).toFixed(2);
        limValue.value = 'R$ ' + v.replace('.', ',').replace(/\B(?=(\d{3})+(?!\d))/g, '.');
      };
      limValue.addEventListener('input', limValue._brlHandler);
      isBrl = true;
    }
  }
  limType.addEventListener('change', toggleValueMode);

  document.querySelector('.adm-page-head')?.insertAdjacentHTML('beforeend',
    '<button class="adm-btn primary" id="btnAddLimit" style="margin-left:auto"><svg width="16" height="16" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><line x1="12" y1="5" x2="12" y2="19"/><line x1="5" y1="12" x2="19" y2="12"/></svg> Impor Limite</button>'
  );

  document.addEventListener('click', e => {
    if (e.target.closest('#btnAddLimit')) {
      ['limUserId','limValue','limNotes'].forEach(id => document.getElementById(id).value = '');
      limType.value = 'deposit';
      document.getElementById('limPeriod').value = 'daily';
      toggleValueMode();
      modal.style.display = 'flex';
    }
    if (e.target.closest('#limCancel') || (e.target === modal)) {
      modal.style.display = 'none';
    }
  });

  document.getElementById('limSave')?.addEventListener('click', async () => {
    var limitVal = isBrl ? parseBRL(limValue.value) : parseInt(limValue.value || 0);
    const body = {
      user_id: document.getElementById('limUserId').value,
      limit_type: limType.value,
      period: document.getElementById('limPeriod').value,
      limit_value: limitVal,
      admin_notes: document.getElementById('limNotes').value
    };
    const r = await fetch('/admin/api/limits', {
      method: 'POST', headers: {'Content-Type':'application/json'},
      body: JSON.stringify(body), credentials: 'same-origin'
    });
    const d = await r.json();
    if (d.ok) { modal.style.display = 'none'; location.reload(); }
    else alert(d.msg || 'Erro');
  });
});
</script>

<%- include('layout-foot') %>
